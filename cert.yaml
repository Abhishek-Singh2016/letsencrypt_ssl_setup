apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: test-duckdns
  namespace: cert-manager
spec:
  secretName: test-duckdns
  dnsNames:
  - "*.abhi-ckad.duckdns.org"
  issuerRef:
    name: duckdns-letsencrypt-prod
    kind: ClusterIssuer